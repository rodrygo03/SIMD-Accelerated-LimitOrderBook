# SIMD-LOB Development Workflow Configuration
# 
# This file contains only development workflow variables.
# Build configuration is now handled by CMake (see CMakePresets.json)
#
# Usage:
#   source ./scripts/load_env.sh
#   cmake --preset=default  # or release, benchmark, etc.

# ============================================================================
# PROJECT STRUCTURE
# ============================================================================

# Project root directory (auto-detected if not set)
PROJECT_ROOT=pwd

# Source directories
SRC_DIR=${PROJECT_ROOT}/src
TEST_DIR=${PROJECT_ROOT}/tests
BENCH_DIR=${PROJECT_ROOT}/bench
SCALAR_DIR=${BENCH_DIR}/scalar

# Build directory (can be overridden by CMake presets)
BUILD_DIR=${PROJECT_ROOT}/build

# ============================================================================
# TEST EXECUTABLES
# ============================================================================

# Test executable paths (relative to BUILD_DIR)
BITSET_TEST_EXEC=${BUILD_DIR}/test_bitset_directory
ORDER_BOOK_TEST_EXEC=${BUILD_DIR}/test_order_book
LOB_ENGINE_TEST_EXEC=${BUILD_DIR}/test_lob_engine
SCALAR_TEST_EXEC=${BUILD_DIR}/bench/scalar/test_scalar
ALL_TESTS_EXEC=${BUILD_DIR}/run_all_tests

# ============================================================================
# OUTPUT DIRECTORIES
# ============================================================================

# Benchmark and test results
RESULTS_DIR=${PROJECT_ROOT}/results
LOGS_DIR=${PROJECT_ROOT}/logs

# Data directories for benchmarks and testing
DATA_DIR=${BENCH_DIR}/data
SYNTHETIC_DATA_DIR=${DATA_DIR}/synthetic
REAL_DATA_DIR=${DATA_DIR}/real

# ============================================================================
# DEVELOPMENT TOOLS
# ============================================================================

# CMake preset to use by default (can be overridden)
DEFAULT_CMAKE_PRESET=default

# Preferred text editor for generated files
EDITOR=${EDITOR:-nano}

# ============================================================================
# NOTES
# ============================================================================
#
# Build Configuration (now in CMake):
#   - Use CMake presets: cmake --preset=<preset-name>
#   - Available presets: default, release, benchmark, penny-stock, high-frequency
#   - Edit CMakePresets.json to add custom configurations
#   - Configuration values are cached in build directory
#
# Quick Commands:
#   ./scripts/build_and_test.sh              # Build with default preset
#   cmake --preset=benchmark && make -C build-benchmark  # Custom preset
#   ctest --preset=default                   # Run tests